# Acceptance evidence — 2026-08-25

Статус: частичный аудит текущего `main` на ревизии `fd43e1ef127e43081fa622864d8699e1822394fe`. Документ фиксирует выполненные проверки и их точные результаты, но не объявляет весь checklist завершённым.

## Автоматические проверки

Из чистого checkout ревизии:

1. `npm test`
2. `npm run build`

Воспроизведённый результат:

- `packages/music-core`: 4 test files, 37 tests passed;
- `apps/web`: 11 test files, 60 tests passed;
- production build прошёл, Vite преобразовал 39 modules и создал статический bundle;
- build выдал предупреждение о размере OSMD chunk больше 500 kB; это не ошибка сборки.

## Ручной Firefox-прогон

Для воспроизведения запустить `npm run dev`, открыть приложение в Firefox, создать или выбрать локальный профиль и менять viewport средствами Responsive Design Mode. Для геометрии сравниваются `document.documentElement.scrollWidth/clientWidth`, границы рабочей области и число доступных клавиш.

### Target `notation`, уровень 6

- `1024×768`: 6 вариантов, 5 нот; нижняя граница последнего основного действия `762 px`; ширина документа `1024/1024` (`scrollWidth/clientWidth`).
- `1180×820`: 6 вариантов, 5 нот; нижняя граница последнего основного действия `751 px`.
- Ошибок в консоли: `0`.

### Target `keyboard`

- `1024×768`: контейнер клавиатуры `966/966`; доступны 38 клавиш; вертикальные границы клавиатуры `y=497–666`.
- `1180×820`: контейнер клавиатуры `1098/1098`; доступны 38 клавиш.
- `320 px`: ширина документа `320/320`; внутренняя ширина фортепиано `1012/268`, поэтому прокрутка локализована внутри клавиатуры и не расширяет страницу.

### Drawer настроек на узком экране

При начальном состоянии drawer закрыт. После открытия фокус переводится внутрь drawer, основной `body` скрыт от взаимодействия; `Escape` закрывает drawer и возвращает фокус на кнопку открытия.

### Настройки только для следующего примера

На текущем примере уровня 6 после переключения уровня сохранились 6 вариантов и прежний prompt. После явного перехода следующий пример был создан уже с 2 вариантами.

### Автопереход

- Правильный ответ: идентификатор текущего примера не изменился на `900 ms` и изменился на `1080 ms`.
- Неправильный ответ: идентификатор не изменился на `1200 ms`.

Эти наблюдения подтверждают задержку примерно в одну секунду только после правильного ответа. Они не заменяют отдельную проверку отмены таймера при каждом предусмотренном teardown-сценарии; это дополнительно покрывается unit-тестами.

## Не выполнено вручную

- Физический MIDI IN/MIDI OUT и hot-plug не проверялись: требуется manual hardware.
- Установка Android, запуск с home screen, standalone/landscape и сохранение профилей origin не проверялись: требуется manual Android device.
- Отдельный большой desktop viewport в этом прогоне не зафиксирован; `1024×768`, `1180×820` и `320 px` не заменяют эту дополнительную проверку.
- Продуктивная запись нот и ритмический тренажёр являются roadmap и не входят в acceptance текущей реализации.
